# User Policy
  Each student login has been restricted to single logon by right clicking the user then properties, then account then logonto following PC
# TempAdmin
  A TempAdmin is created which is local admin of all pc
  Then user can be added to TempAdmin so that they become admin of their individual PC
